Task: Your task is to convert the provided mathematic exam content in LaTeX format into a structured JSON format according to the schema.

Requirements:
- Single number must also be placed inside equation mode, e.g., $1$, $3.14$, $75\%$, etc.
- Remove all unnecessary texts like website links, text inputs, footers, page indexes, separators, etc.
- Exclude all figures, images, tables, and diagrams.
- Remove all styling commands like bold, italics, underline, color, font size, etc.
- Ensure all mathematical expressions are correctly formatted in LaTeX.

Output JSON schema:
{format_instructions}

Input Exam:
```latex
{exam_content}
```